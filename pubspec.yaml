name: deal
description: "Ndokoti — Marketplace de deals Afrique Centrale"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  image_picker: ^1.1.2
  http: ^1.2.2
  shared_preferences: ^2.3.2
  # Sprint 2 — décommenter quand prêt
  # supabase_flutter: ^2.5.0
  # speech_to_text: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/deals_data.json
